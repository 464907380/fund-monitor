# Reasonix configuration.
# Resolution order: flag > ./reasonix.toml > ~/.config/reasonix/config.toml > built-in defaults.
# Secrets come from the environment via api_key_env; never put keys here.

config_version = 2   # schema marker for diagnostics; old versions may ignore it
default_model = "deepseek-flash"
language      = "zh"   # ui/model language; empty = auto-detect from $LANG / $REASONIX_LANG

[network]
proxy_mode = "auto"   # auto|env|custom|off; auto currently uses env proxy
# proxy_url  = "socks5://127.0.0.1:7890"   # optional custom override
# no_proxy   = "localhost,127.0.0.1,.local"   # honored for proxy_mode = "custom"

[network.proxy]
type = "socks5"   # http|https|socks5|socks5h
# server = "127.0.0.1"
# port = 7890
# username = ""
# password = "${REASONIX_PROXY_PASSWORD}"   # optional; supports ${VAR} expansion

[agent]
# system_prompt = """..."""   # omit to use the built-in prompt for this version
system_prompt_file = "prompts/system.md"
max_steps         = 0   # executor tool-call rounds; 0 = no limit
planner_max_steps = 12   # planner read-only tool-call rounds; 0 = no limit
temperature       = 0.0
auto_plan   = "on"   # off|on; off keeps plan mode manual
# auto_plan_classifier = "deepseek-flash"   # optional; only used for borderline tasks
soft_compact_ratio  = 0.5   # notice only; keeps cache-first prefix intact
compact_ratio       = 0.8   # try compacting when prompt reaches this fraction
compact_force_ratio = 0.9   # force compacting at this high-water mark
# planner_model = "mimo"   # optional: enable two-model collaboration
# subagent_model = "deepseek-pro"   # optional default for runAs=subagent skills
# subagent_models = { review = "deepseek-pro", security_review = "deepseek-pro" }   # per-skill overrides
# subagent_effort = "high"   # optional default effort for subagents
# subagent_efforts = { review = "max", task = "high" }   # per-tool/skill effort overrides
output_style = "explanatory"   # persona/tone folded into the prompt

[[providers]]
name        = "deepseek-flash"
kind        = "openai"
base_url    = "https://api.deepseek.com"
models      = ["deepseek-v4-flash"]
default     = "deepseek-v4-flash"
api_key_env = "DEEPSEEK_API_KEY"
balance_url = "https://api.deepseek.com/user/balance"   # optional; wallet-balance endpoint shown in the status bar
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.02, input = 1, output = 2, currency = "¥" }   # per 1M tokens

[[providers]]
name        = "deepseek-pro"
kind        = "openai"
base_url    = "https://api.deepseek.com"
model       = "deepseek-v4-pro"
api_key_env = "DEEPSEEK_API_KEY"
balance_url = "https://api.deepseek.com/user/balance"   # optional; wallet-balance endpoint shown in the status bar
context_window = 1000000   # tokens; compaction triggers near this limit
price       = { cache_hit = 0.025, input = 3, output = 6, currency = "¥" }   # per 1M tokens

[tools]
enabled = []   # empty = all built-in tools
bash_timeout_seconds = 120   # foreground safety cap; set 0 for no tool-local cap

[codegraph]
enabled      = true   # built-in MCP server; off by default for first-run sessions
auto_install = true   # fetch the runtime when CodeGraph is enabled but missing
# path       = ""   # empty = cache, then PATH, then a bundle beside reasonix

[lsp]
enabled = true   # language server tools; servers launch lazily when used

[lsp.servers.python]
command = "pyright"
extensions = [".py"]

[skills]
# paths = ["~/my-skills", "../shared/skills"]   # extra custom skill roots
# excluded_paths = ["~/.agents/skills"]   # hide convention roots without deleting folders
# max_depth = 3   # nested scan depth; set 1 for legacy root-only discovery
# disabled_skills = ["review"]   # hide noisy or unwanted skills

[permissions]
# Per-call gating. mode = writer fallback when no rule matches: ask|allow|deny.
# Readers always default to allow. Precedence: deny > ask > allow > fallback.
# Rules are "Tool" or "Tool(specifier)"; e.g. Bash(go test:*), Edit(src/**).
mode  = "allow"
deny = ["mcp__unity-mcp__*"]
allow = ["Bash(*)", "Bash(python -c @'\nimport re\nfrom fund_utils import _strip_html\n\nfund_alerts = [\n    \"🔴 <font color=\\\"warning\\\">招商中证白酒(161725) 急跌 -3.2%（当前-0.11%）</font>\",\n    \"🟢 <font color=\\\"info\\\">诺安成长混合(320007) 急涨 +5.1%（当前+6.20%）</font>\",\n]\nstock_groups = {\n    \"招商中证白酒\": (\"161725\", [\n        \"🔴 <font color=\\\"warning\\\">招商中证白酒持仓贵州茅台(600519)急跌 -5.3%（当前跌4.20%，占比3.5%）</font>\"\n    ]),\n    \"诺安成长混合\": (\"320007\", [\n        \"🟢 <font color=\\\"info\\\">诺安成长混合持仓宁德时代(300750)急涨 +7.2%（当前涨5.10%，占比5.0%）</font>\"\n    ]),\n}\n\ndef _icon_text(raw):\n    icon = \"🔴\" if raw.startswith(\"🔴\") else \"🟢\"\n    text = _strip_html(raw)\n    return icon, text\n\nlines = []\nfor fund_name, (fund_code, s_alerts) in sorted(stock_groups.items()):\n    lines.append(\"\")\n    lines.append(f\"**{fund_name}（{fund_code}）**\")\n    matched_fa = [a for a in fund_alerts if fund_name in a]\n    if matched_fa:\n        for a in matched_fa:\n            icon, text = _icon_text(a)\n            lines.append(f\"  {icon} 基金：{text}\")\n    for a in s_alerts:\n        icon, text = _icon_text(a)\n        clean = text.split(\"持仓\", 1)[-1] if \"持仓\" in a else text\n        lines.append(f\"  {icon} 持股·{clean}\")\n\nprint(\"\\n\".join(lines))\n'@)", "Bash(python -c '\nfrom fund_monitor import _strip_html\nimport re\n\n# 测试去掉基金名前缀\ntests = [\n    \"招商中证白酒(161725) 急跌 -3.2%（当前-0.11%）\",\n    \"诺安成长混合(320007) 急涨 +5.1%（当前+6.20%）\",\n    \"财通成长优选(001480) 当日累计跌 -3.2%（0.00%→-3.20%）\",\n]\nfor t in tests:\n    clean = re.sub(r\"^.+?\\d{6}\\)\\s*\", \"\", t)\n    print(f\"  {clean}\")\n')", "Bash(python -c @'\nimport re\nfrom fund_utils import _strip_html\n\nfund_alerts = [\n    \"🔴 <font color=\\\"warning\\\">招商中证白酒(161725) 急跌 -3.2%（当前-0.11%）</font>\",\n    \"🟢 <font color=\\\"info\\\">诺安成长混合(320007) 急涨 +5.1%（当前+6.20%）</font>\",\n]\nstock_groups = {\n    \"招商中证白酒\": (\"161725\", [\n        \"🔴 <font color=\\\"warning\\\">招商中证白酒持仓贵州茅台(600519)急跌 -5.3%（当前跌4.20%，占比3.5%）</font>\"\n    ]),\n    \"诺安成长混合\": (\"320007\", [\n        \"🟢 <font color=\\\"info\\\">诺安成长混合持仓宁德时代(300750)急涨 +7.2%（当前涨5.10%，占比5.0%）</font>\"\n    ]),\n}\n\nfor fund_name, (fund_code, s_alerts) in sorted(stock_groups.items()):\n    print(f\"**{fund_name}（{fund_code}）**\")\n    matched_fa = [a for a in fund_alerts if fund_name in a]\n    for a in matched_fa:\n        text = _strip_html(a)\n        if text.startswith(\"🔴\") or text.startswith(\"🟢\"):\n            text = text[1:].strip()\n        icon = \"🔴\" if \"急跌\" in text or \"累计跌\" in text else \"🟢\"\n        clean_fa = re.sub(r\"^.+?\\d{6}\\)\\s*\", \"\", text)\n        print(f\"  {icon} 基金：{clean_fa}\")\n    for a in s_alerts:\n        text = _strip_html(a)\n        if text.startswith(\"🔴\") or text.startswith(\"🟢\"):\n            text = text[1:].strip()\n        icon = \"🔴\" if \"急跌\" in text or \"累计跌\" in text else \"🟢\"\n        clean = text.split(\"持仓\", 1)[-1]\n        print(f\"  {icon} 持股·{clean}\")\n    print()\n'@)", "Bash(git add -A && git commit -m \"feat: 盘中警报按基金分组统一展示\n\n改前：基金警报和个股警报分开两个区块，用户需要自己配对\n改后：每只基金一个区块，基金警报+个股警报放在一起：\n\n**招商中证白酒（161725）**\n  🔴 基金：急跌 -3.2%（当前-0.11%）\n  🔴 持股·贵州茅台(600519)急跌 -5.3%（占比3.5%）\n\n- stock_groups 改为 dict[str, tuple[str, list[str]]] 携带基金代码\n- 标题自动匹配基金名，图标从内容方向推断\" 2>&1 | Select-Object -Last 3)"]
# ask = ["Edit(src/**)"]   # force a prompt even if otherwise allowed

[sandbox]
# Confine tool blast radius. File-writers (write_file/edit_file/multi_edit)
# may only write under workspace_root (empty = current dir) + allow_write.
# bash = "enforce" (default) jails each command in an OS sandbox (macOS now;
# graceful fallback elsewhere); "off" disables it. network allows egress.
# workspace_root = ""            # default: current working directory
# allow_write = ["/tmp"]          # extra dirs writers may also modify
bash    = "off"
network = true

[statusline]
# A custom status line: a command whose first stdout line replaces the built-in
# data row. It receives {"model","contextUsed","contextWindow","cwd"} as JSON on stdin.
# command = "my-statusline.sh"

# External MCP servers. type: "stdio" (default, a subprocess) | "http" | "sse".
# ${VAR} / ${VAR:-default} are expanded from the environment in command/args/env/url/headers.
# [[plugins]]
# name    = "example"
# command = "reasonix-plugin-example"
# [[plugins]]                                  # a remote server over Streamable HTTP
# name    = "stripe"
# type    = "http"
# url     = "https://mcp.stripe.com"
# headers = { Authorization = "Bearer ${STRIPE_KEY}" }
